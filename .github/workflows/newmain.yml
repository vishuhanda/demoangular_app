name: Jobs for practice

on: [push]


jobs:
    my_first_job:
      runs-on: ubuntu-latest
      steps:
      - name: first step
        id : first_step
        env : 
          GREETING : hello
        run : |
          echo $GREETING

